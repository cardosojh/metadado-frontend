import React, { Component } from 'react';
import Typography from '@material-ui/core/Typography';

export default class Notfound extends Component {
    render() {
      return (
        <center>            
            <Typography gutterBottom>Página não encontrada.</Typography>
        </center>
      );
    }
};